import React from 'react';
import RestrictedContainer from './RestrictedContainer';

const Internal = () => {
	return (
		<RestrictedContainer>
			<div>Logged In</div>
		</RestrictedContainer>
	)
}

export default Internal;